{
  "name": "Space-full-stack",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "setup": "pnpm install",
    "server": "pnpm --dir server run watch",
    "client": "pnpm --dir client run start",
    "watch": "pnpm run server & pnpm run client",
    "deploy": "pnpm --dir client run build && pnpm --dir server start",
    "deploy-cluster": "pnpm --dir client run build && pnpm --dir server run cluster",
    "test": "pnpm --dir server test && pnpm --dir client test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "11.9.0",
      "onFail": "error"
    }
  }
}
